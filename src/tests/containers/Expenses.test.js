import React from 'react';
import ReactDOM from 'react-dom';
import expect from 'expect';
import $ from 'jquery';
import TestUtils from 'react-addons-test-utils';

import Expenses from 'Expenses';

describe('Expenses', () => {
  it('should exist', () => {
    expect(Expenses).toExist();
  });
});
